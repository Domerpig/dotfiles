local lsp = require('lspconfig')

-- lsp.rust_analyzer.setup({
    -- flags = flags,
    -- capabilities = capabilities,
    -- on_attach = on_attach,
    -- settings = {
        -- ['rust-analyzer'] = {
            -- cargo = {
                -- allFeatures = true,
            -- },
            -- checkOnSave = {
                -- allFeatures = true,
                -- command = 'clippy',
            -- },
        -- },
    -- },
-- })
